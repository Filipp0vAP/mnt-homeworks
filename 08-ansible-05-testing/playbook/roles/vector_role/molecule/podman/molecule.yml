---
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: centos-8-podman
    image: quay.io/centos/centos:stream8
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
